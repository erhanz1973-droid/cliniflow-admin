# Render Blueprint (optional): Static Site — HTML/CSS/JS in ./public
# Dashboard: create a Static Site, set Publish directory to `public` (or use this Blueprint).
services:
  - type: web
    name: cliniflow-admin
    runtime: static
    region: oregon
    buildCommand: "echo static"
    staticPublishPath: ./public
